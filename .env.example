TRANSPORT=telegram    # telegram, tui, web, or unified
TELEGRAM_BOT_TOKEN=   # required when TRANSPORT=telegram
TELEGRAM_POLISH_BOT_TOKEN=   # required when TRANSPORT=unified
TELEGRAM_SPANISH_BOT_TOKEN=  # required when TRANSPORT=unified
PROVIDER=openrouter   # openrouter (default); use ollama only if an Ollama/OpenAI-compatible server is reachable
OPENROUTER_API_KEY=   # required when PROVIDER=openrouter
CHAT_MODEL=google/gemini-3.1-flash-lite-preview  # fast model for live conversation turns
EVALUATOR_MODEL=deepseek/deepseek-v4-flash       # smarter model for post-turn evaluation, dreams, and practice grading; runs async so latency doesn't matter
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2
OLLAMA_API_KEY=       # optional, for authenticated Ollama proxies
DB_PATH=./data/buddy.db
DATA_DIR=./data       # web/unified mode stores per-language DBs and memory under this directory
WEB_HOST=127.0.0.1    # web/unified mode only; use 0.0.0.0 to expose on LAN/tunnel
WEB_PORT=8787         # web/unified mode only
ALLOWED_USERS=
MORNING_CRON=0 9 * * *
EVENING_CRON=30 19 * * *
TIMEZONE=Europe/Warsaw
TELEGRAM_CHAT_ID=      # required when TRANSPORT=telegram or TRANSPORT=unified
